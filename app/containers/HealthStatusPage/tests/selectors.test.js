// import { selectHealthStatusPageDomain } from '../selectors';

describe('selectHealthStatusPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
