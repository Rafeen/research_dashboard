// import { selectDataTablePageDomain } from '../selectors';

describe('selectDataTablePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
